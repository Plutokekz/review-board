{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'grep -qs \"autoReview: true\" .claude/review-board.local.md || exit 0; git rev-parse --is-inside-work-tree >/dev/null 2>&1 || exit 0; [ -z \"$(git status --porcelain 2>/dev/null)\" ] && exit 0; echo \"review-board: changes detected — run /review to open a review.\"'"
          }
        ]
      }
    ]
  }
}
